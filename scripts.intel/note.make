Note for make

OTF library is built as shared library and as static library.
Use shared library for user programs.

For some reason, OTF static library libopen-trace-format.a does not
link in the zlib package correctly.

